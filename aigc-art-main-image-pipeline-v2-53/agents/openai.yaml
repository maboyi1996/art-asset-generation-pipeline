interface:
  display_name: "AIGC Art Main Image Pipeline V2.53"
  short_description: "Build main-image production and review MD tables from Chinese scripts with corrected B/C character multi-segment continuity rating."
  default_prompt: "Use $aigc-art-main-image-pipeline-v2-53 to read a Chinese script, produce scene coverage, extract character/scene/prop assets, detect visible state variants, rate asset continuity with single-episode multi-segment B/C character rules, generate global task drafts for all retained production assets, build a character disposition ledger, create character identity anchors, audit production readiness, generate main-image prompts, and output main-image-production-table.md plus main-image-review-table.md."

policy:
  allow_implicit_invocation: true

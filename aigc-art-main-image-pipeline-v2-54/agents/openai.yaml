interface:
  display_name: "AIGC Art Main Image Pipeline V2.54"
  short_description: "Build main-image MD tables with stricter scene, character, and state validation."
  default_prompt: "Use $aigc-art-main-image-pipeline-v2-54 to read a Chinese script, produce scene coverage, extract character/scene/prop assets, detect visible state variants without scene-level state leakage, rate asset continuity with B/C character rules, generate global task drafts for all retained production assets, build a character disposition ledger, audit production readiness, generate main-image prompts, and output main-image-production-table.md plus main-image-review-table.md."

policy:
  allow_implicit_invocation: true

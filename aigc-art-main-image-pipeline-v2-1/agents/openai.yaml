interface:
  display_name: "AIGC Art Main Image Pipeline V2.1"
  short_description: "Build complete machine and derived human review MD tables for main-image production."
  default_prompt: "Use $aigc-art-main-image-pipeline-v2-1 to identify character, scene, and prop assets by episode/scene, merge them into complete global asset-state tasks, build key-character identity anchors, audit omissions and state variants, then produce main-image-production-table.md for AI/downstream parsing and main-image-review-table.md for human review."

policy:
  allow_implicit_invocation: true

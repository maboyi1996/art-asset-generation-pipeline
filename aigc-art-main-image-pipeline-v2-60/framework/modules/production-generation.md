# 模块：资产生产与提示词生成

## 作用

基于全局任务草稿建立角色锚点，做生产前一致性审计，并为可生产任务生成主图提示词或状态编辑指令。

## 层级顺序

`character-anchor.md -> production-audit.md -> prompt-generation.md`

## 边界

本模块读取前序资产、状态、评级、去向、任务草稿和映射结果，不重新抽取资产、不重新拆状态、不重新评级、不补建任务。

interface:
  display_name: "AIGC Art Main Image Pipeline V2.0"
  short_description: "Build MD-only main-image outputs: a machine production table and a compact human review table."
  default_prompt: "Use $aigc-art-main-image-pipeline-v2-0 to identify character, scene, and prop assets by episode/scene, merge them into global asset-state tasks, build key-character identity anchors, audit omissions and state variants, then produce main-image-production-table.md for AI/downstream parsing and main-image-review-table.md for human review."

policy:
  allow_implicit_invocation: true

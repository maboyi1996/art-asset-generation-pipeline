interface:
  display_name: "AIGC Art Main Image Pipeline V2.63"
  short_description: "Build main-image JSON plus Markdown audit tables."
  default_prompt: "Use $aigc-art-main-image-pipeline-v2-63 to read a Chinese script, build lossless character/scene/prop evidence ledgers, rate base assets before state splitting, enforce hard S/A/B/C routes, and deliver prompt-manifest.json plus main-image-production-table.md and main-image-review-table.md."

policy:
  allow_implicit_invocation: true

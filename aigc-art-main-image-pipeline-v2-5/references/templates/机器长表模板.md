# 机器长表模板

## 文件名

`main-image-production-table.md`

## 必须包含

1. 项目概览。
2. 角色身份锚点表。
3. 全局生产任务表。
4. 分集使用映射表。
5. 生产审计摘要。

## 全局生产任务表字段

| 字段 |
|---|
| `task_id` |
| `asset_type` |
| `asset_name` |
| `state_name` |
| `priority_level` |
| `same_asset_evidence` |
| `task_type` |
| `anchor_task_id` |
| `review_status` |
| `review_reason` |
| `episode_id` |
| `source_evidence` |
| `prompt` |
| `edit_instruction` |
| `negative_prompt` |
| `review_focus` |

字段只使用上表列举字段；新增字段必须先更新 `任务类型字段审核规则.md`。

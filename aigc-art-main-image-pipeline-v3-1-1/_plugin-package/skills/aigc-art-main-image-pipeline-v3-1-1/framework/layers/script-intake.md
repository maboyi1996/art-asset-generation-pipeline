# script-intake.md

## 作用

读取原始剧本/PDF，建立稳定的项目、集、场、原文块和来源定位。

## 输入

原始剧本/PDF。

## 调用文件

`references/rules/剧本解析输出规则.md`

## 输出结果

`audit/script-intake-result.md`

## 边界

只做解析和来源定位，不抽取资产、不拆状态、不评级。疑似场次标题、闪回标记和场号变体必须在本层解决或标记；后续层级不得重新切场来修复解析错误。

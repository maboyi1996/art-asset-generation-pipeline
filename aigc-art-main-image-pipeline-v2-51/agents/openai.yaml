interface:
  display_name: "AIGC Art Main Image Pipeline V2.51"
  short_description: "Build main-image production and review MD tables from Chinese scripts with explicit layer contracts and character disposition closure."
  default_prompt: "Use $aigc-art-main-image-pipeline-v2-51 to read a Chinese script, produce scene coverage, extract character/scene/prop assets, detect visible state variants, rate asset continuity, build a global task registry and character disposition ledger, create character identity anchors, audit production readiness, generate main-image prompts, and output main-image-production-table.md plus main-image-review-table.md."

policy:
  allow_implicit_invocation: true

interface:
  display_name: "AIGC Art Main Image Pipeline V1.2"
  short_description: "从剧本生成可审核、可批量生图的美术主图生产表"
  default_prompt: "Use $aigc-art-main-image-pipeline-v1-2 to extract character, scene, and prop main-image assets from this script, build key-character identity anchors before episode state prompts, audit omissions, timeline/state variants, and per-episode asset usage, then produce main-image-production-table.md and prompt-manifest.json."

policy:
  allow_implicit_invocation: true
